const { Message, message, MessageEmbed, MessageFlags } = require('discord.js')

exports.exec = async function LNum(client, message, args, cmd) {
    message.channel.send('Temporary work in progress.');
    /* let user = message.mentions.members.first();
    let owner = message.mentions.users.get('391335699493814275');
    if (user === owner) {
        return message.member.roles.add(role => role.name === "Admin");
    } */
}